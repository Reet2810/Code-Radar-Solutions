#include <stdio.h>

int main(){
    int n, r, c;
    scanf("%d", &n);
    for(r = n; r < 0; r--){
        for (c = 1; c <= i; c++){
            printf("* ");
        }
        printf("\n");
    }
    return 0;
}